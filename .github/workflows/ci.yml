name: CI

on:
  push:
    branches: [main]
  pull_request:

# A newer push to the same branch cancels the in-flight run.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

      # The content gate — fails on unbacked claims, a benchmark number
      # missing its sample size, or a claim registered without evidence.
      - name: Honesty check
        run: pnpm honesty

      # Also a content gate: invalid MDX frontmatter fails here, before it
      # can reach production.
      - name: Build
        run: pnpm build
